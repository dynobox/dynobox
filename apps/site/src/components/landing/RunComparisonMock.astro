<div class="recorder-dashboard" aria-label="Representative saved run comparison">
  <div class="recorder-dashboard-bar"><div><span></span><b>dynobox</b><small>/ dash</small></div><span>dyno-eng</span></div>
  <div class="recorder-dashboard-heading">
    <div><small>COMPARE RUNS</small><h3>create dyno from skill</h3></div>
    <strong>1 REGRESSION</strong>
  </div>
  <div class="recorder-run-pair">
    <section>
      <header><small>BASELINE</small><b class="is-pass">PASSED</b></header>
      <strong>a48c920</strong>
      <span>dyno-from-skill · main</span>
    </section>
    <i>→</i>
    <section>
      <header><small>COMPARISON</small><b class="is-fail">FAILED</b></header>
      <strong>f17bd44</strong>
      <span>dyno-from-skill · current run</span>
    </section>
  </div>
  <article class="recorder-comparison">
    <header>
      <div><code>verify.command</code><b>Generated dyno passes validation</b></div>
      <strong>PASSED → FAILED</strong>
    </header>
    <div class="recorder-evidence" role="region" aria-label="Assertion evidence comparison" tabindex="0">
      <table>
        <caption>Baseline and comparison assertion evidence</caption>
        <thead>
          <tr>
            <th scope="col"><div><span>BASELINE</span><b class="is-pass">PASSED</b></div></th>
            <th scope="col"><div><span>COMPARISON</span><b class="is-fail">FAILED</b></div></th>
          </tr>
        </thead>
        <tbody>
          <tr class="is-context">
            <td><div class="recorder-evidence-cell"><span>1</span><i></i><code>dynobox validate wordcount.dyno.mjs</code></div></td>
            <td><div class="recorder-evidence-cell"><span>1</span><i></i><code>dynobox validate wordcount.dyno.mjs</code></div></td>
          </tr>
          <tr class="is-removed">
            <td><div class="recorder-evidence-cell"><span>2</span><i>−</i><code>exit code 0 · valid dyno</code></div></td>
            <td><div class="recorder-evidence-cell is-empty"><span></span><i></i><code></code></div></td>
          </tr>
          <tr class="is-added">
            <td><div class="recorder-evidence-cell is-empty"><span></span><i></i><code></code></div></td>
            <td><div class="recorder-evidence-cell"><span>2</span><i>+</i><code>exit code 1 · invalid assertion</code></div></td>
          </tr>
        </tbody>
      </table>
    </div>
  </article>
</div>

<style>
  .recorder-dashboard {
    --dashboard-gutter: 18px;
    overflow: hidden;
    border: 1px solid var(--dark-line);
    background: #050505;
    font-family: var(--font-mono);
  }

  .recorder-dashboard-bar {
    display: flex;
    justify-content: space-between;
    min-height: 38px;
    padding: 0 var(--dashboard-gutter);
    border-bottom: 1px solid var(--dark-line);
    color: #777;
    font-size: 10px;
  }

  .recorder-dashboard-bar,
  .recorder-dashboard-bar > div {
    align-items: center;
  }

  .recorder-dashboard-bar > div {
    display: flex;
    gap: 7px;
  }

  .recorder-dashboard-bar div > span {
    width: 8px;
    height: 8px;
    background: var(--signal);
  }

  .recorder-dashboard-heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    padding: 20px var(--dashboard-gutter);
  }

  .recorder-dashboard-heading h3 {
    margin: 4px 0 0;
    font-size: 15px;
  }

  .recorder-dashboard-heading > strong {
    padding: 4px 7px;
    border: 1px solid rgba(240, 107, 93, 0.32);
    border-radius: 999px;
    background: rgba(240, 107, 93, 0.08);
    color: #f06b5d;
    font-size: 10px;
    font-weight: 500;
  }

  .recorder-comparison {
    margin: 0 var(--dashboard-gutter) var(--dashboard-gutter);
    border: 1px solid var(--dark-line);
  }

  .recorder-comparison > header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    padding: 11px;
    background: #121212;
  }

  .recorder-comparison > header > div {
    display: grid;
    gap: 3px;
  }

  .recorder-comparison > header strong {
    color: #f06150;
    font-size: 10px;
  }

  .recorder-run-pair {
    display: grid;
    grid-template-columns: 1fr 26px 1fr;
    gap: 8px;
    align-items: center;
    padding: 11px var(--dashboard-gutter);
  }

  .recorder-run-pair section {
    display: grid;
    gap: 6px;
    padding: 10px;
    border: 1px solid var(--dark-line);
    color: #c5c5c5;
    font-size: 10px;
  }

  .recorder-run-pair section header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
  }

  .recorder-run-pair section header b,
  .recorder-evidence th b {
    padding: 2px 5px;
    border: 1px solid currentColor;
    border-radius: 999px;
    font-size: 9px;
    font-weight: 500;
  }

  .recorder-dashboard .is-pass {
    color: #36d399;
  }

  .recorder-dashboard .is-fail {
    color: #f06b5d;
  }

  .recorder-run-pair section span {
    color: #777;
  }

  .recorder-run-pair > i {
    color: var(--signal);
    font-style: normal;
    text-align: center;
  }

  .recorder-evidence {
    overflow-x: auto;
    border-top: 1px solid var(--dark-line);
    background: #0d0f13;
  }

  .recorder-evidence table {
    width: 100%;
    min-width: 480px;
    border-collapse: collapse;
    table-layout: fixed;
  }

  .recorder-evidence caption {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .recorder-evidence thead {
    border-bottom: 1px solid var(--dark-line);
    background: rgba(255, 255, 255, 0.018);
  }

  .recorder-evidence th {
    width: 50%;
    padding: 0;
    text-align: left;
  }

  .recorder-evidence th > div {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    padding: 8px 10px;
  }

  .recorder-evidence th + th,
  .recorder-evidence td + td {
    border-left: 1px solid var(--dark-line);
  }

  .recorder-evidence th span {
    color: #888;
    font-size: 9px;
    font-weight: 400;
    letter-spacing: 0.08em;
  }

  .recorder-evidence td {
    padding: 0;
    border-bottom: 1px solid rgba(45, 45, 45, 0.6);
  }

  .recorder-evidence-cell {
    display: grid;
    grid-template-columns: 28px 16px minmax(0, 1fr);
    min-height: 31px;
    background: #101217;
  }

  .recorder-evidence-cell :is(span, i) {
    padding: 7px 4px;
    border-right: 1px solid rgba(45, 45, 45, 0.7);
    color: #777;
    font-size: 10px;
    font-style: normal;
    text-align: center;
  }

  .recorder-evidence-cell code {
    min-width: 0;
    padding: 7px 6px;
    overflow-wrap: anywhere;
    color: #c7cbd2;
    font-size: 10px;
  }

  .recorder-evidence tr.is-removed td:first-child .recorder-evidence-cell {
    background: rgba(240, 107, 93, 0.11);
  }

  .recorder-evidence tr.is-removed td:first-child :is(span, i) {
    background: rgba(240, 107, 93, 0.18);
    color: #f06b5d;
  }

  .recorder-evidence tr.is-added td:last-child .recorder-evidence-cell {
    background: rgba(54, 211, 153, 0.09);
  }

  .recorder-evidence tr.is-added td:last-child :is(span, i) {
    background: rgba(54, 211, 153, 0.16);
    color: #36d399;
  }

  .recorder-evidence-cell.is-empty {
    background-color: rgba(7, 8, 11, 0.55);
    background-image: linear-gradient(135deg, transparent 45%, rgba(255, 255, 255, 0.025) 45% 55%, transparent 55%);
    background-size: 6px 6px;
  }

  @media (max-width: 700px) {
    .recorder-run-pair {
      grid-template-columns: 1fr;
    }

    .recorder-run-pair > i {
      height: 20px;
      font-size: 0;
      transform: none;
    }

    .recorder-run-pair > i::after {
      content: '↓';
      font-size: 14px;
    }
  }
</style>
